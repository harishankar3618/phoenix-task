# Phoenix Advisory — Frontend Scaffold

This workspace contains a minimal Next.js + Tailwind scaffold for the homepage redesign.

Quick start:

```bash
npm install
npm run dev
```

Next steps:
- Implement components in `src/components`.
- Add sections in `src/app/page.tsx`.
- Wire i18n and WordPress API client.
